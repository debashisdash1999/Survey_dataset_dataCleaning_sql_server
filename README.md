# Survey_dataset_dataCleaning_sql_server
SQL Server data cleaning project — messy 106-row customer survey dataset transformed into a production-ready table using T-SQL. Covers CTE-based duplicate removal, multi-format date normalization, NULL imputation with audit flag, controlled vocabulary mapping for categorical columns, and whitespace cleanup. Raw source preserved as audit layer.
